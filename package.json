{
  "name": "spolia",
  "version": "1.0.0",
  "description": "Hyperlocal reclaimed construction materials marketplace",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npx serve . --listen 3000 --cors",
    "demo": "npx serve . --listen 3000 --cors --open"
  },
  "private": true
}
